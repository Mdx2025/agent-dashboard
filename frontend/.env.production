# Production Configuration for Railway
# Update these URLs with your actual Railway backend URLs

# API URL (HTTP for REST endpoints)
VITE_API_URL=https://agent-dashboard-backend-production.up.railway.app

# WebSocket URL (WSS for WebSocket connections)
VITE_WS_URL=wss://agent-dashboard-backend-production.up.railway.app

# Note: These are placeholder URLs. Update with your actual Railway deployment URLs.
# You can find them in your Railway project dashboard after deployment.
